//const { people, ages } = require('./people'); //returns an object, empty or not
//console.log(people, ages);

//const os = require('os'); //built in to node
//console.log(os.platform(), os.homedir());